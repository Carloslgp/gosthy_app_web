# Opcional: URL do Cloud Mock do Apidog consumida por GET /api/pessoas.
# Sem esta variavel a Azure Function usa o mock local embutido — o projeto
# funciona normalmente sem configurar nada.
mock_api_url=
